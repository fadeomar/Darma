export default function Article() {
  return (
    <div className="space-y-4 text-sm leading-7 text-[var(--color-text-muted)]">
      <p>
        CSS <code>box-shadow</code> can create subtle elevation, inset surfaces, focus states, and layered design effects without images.
      </p>
      <p>
        Use smaller blur and opacity for production UI shadows, and reserve many large layers for hero artwork or static marketing surfaces.
      </p>
    </div>
  );
}
