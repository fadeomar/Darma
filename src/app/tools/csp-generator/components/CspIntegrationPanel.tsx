export function CspIntegrationPanel() { return <section className="rounded-[var(--radius-md)] border border-[var(--color-border)] bg-[var(--color-bg-soft)] p-3 text-xs leading-5 text-[var(--color-text-soft)]"><h3 className="text-sm font-bold text-[var(--color-text)]">Deployment guidance</h3><p className="mt-1">Prefer the HTTP header for production. Meta tags support fewer CSP features and cannot reliably replace server headers. Test in Report-Only mode before enforcing a policy.</p></section>; }
